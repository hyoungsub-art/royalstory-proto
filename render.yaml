# Render.com 블루프린트 — 킹덤 크래프트 PvP 릴레이 (무료 티어)
# 사용법: Render 대시보드 → New → Blueprint → royalstory-proto 저장소 선택
services:
  - type: web
    name: kingdomcraft-relay
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
